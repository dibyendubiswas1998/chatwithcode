# logs:
logs:
  log_file: logs/logs.log

# artifacts:
artifacts:
  artifacts_dir: artifacts
  # data:
  data:
    data_dir: artifacts/data
    github_data: artifacts/data/github
  # vector DB:
  vectordb:
    chromadb_dir: artifacts/chromadb
  # chatdata:
  chatdata: artifacts/chatdata.json